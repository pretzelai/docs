---
title: "Linear"
description: "Assign Linear tickets to Cord and get PRs back automatically."
---

<Info>
Linear integration is available on the **Pro** and **Enterprise** plans.
</Info>

Connect Cord to Linear so your team can assign tickets that Cord picks up and works on autonomously.

## Setup

1. Go to **Settings > Linear**
2. Click **Connect Linear**
3. Authorize the Cord app in your Linear workspace

## Team mapping

After connecting, Cord auto-maps team members by matching email addresses between GitHub and Linear. Check mapping status in the Linear settings tab.

If mappings get out of sync (new team members, changed emails), click **Re-sync Members** to refresh.

### Personal Linear linking

Each team member can link their personal Linear account in settings. This ensures tickets and activity are attributed correctly.

## Usage

Assign a Linear issue to **Cord** (it appears as an assignee in your workspace). Cord will:

1. Read the issue title, description, and any linked context
2. Route to the correct repository
3. Spin up a sandbox and start an agent
4. Post updates on the issue as work progresses
5. Link the PR to the issue when done

## What makes a good Linear ticket for Cord

Cord works best with tickets that are:

- **Specific** -- "Add a /health endpoint to the API" vs "Improve the backend"
- **Self-contained** -- everything the agent needs is in the ticket
- **Scoped** -- one clear deliverable, not a multi-week epic

Include code references, screenshots, and acceptance criteria in the ticket description. The agent reads all of it.

## Repo routing

Cord uses AI to determine which repository a Linear ticket should be routed to, based on:

- The ticket description and labels
- Your team's previous routing patterns
- Project and team associations in Linear

If routing is ambiguous, Cord will ask for clarification.

## Status updates

As the agent works, Cord updates the Linear issue:

| Event | Linear status |
|---|---|
| Agent starts working | **In Progress** |
| Progress updates | Comments on the issue |
| PR created | PR linked to the issue |
| PR merged | **Done** |

## Disconnecting

Admins can disconnect Linear in **Settings > Linear**. Individual members can unlink their personal Linear account without disconnecting the org-level integration.
